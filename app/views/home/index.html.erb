<div class="row">
  <div class="six columns">
    <h3>Swaps</h3>
    <ol>
      <% @groups.each do |group| %>
        <li><%= link_to group.name, group_path(group) %></li>
      <% end %>
    </ol>
  </div>

</div>